<Window x:Class="WpfApp15.AttendanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp15"
        mc:Ignorable="d"
        Title="Посещаемость" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Студент:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CbStudents" Grid.Row="0" Grid.Column="1" Margin="4" DisplayMemberPath="FullName" SelectedValuePath="Id"/>

        <TextBlock Text="Дата:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DpDate" Grid.Row="1" Grid.Column="1" Margin="4"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0 6 0 0">
            <CheckBox x:Name="CbIsPresent" Content="Присутствовал" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Text="Примечание:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0 6 0 0"/>
        <TextBox x:Name="TbNote" Grid.Row="3" Grid.Column="1" Margin="4" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0 8 0 0">
            <Button Content="OK" Width="80" Margin="0 0 8 0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
